# Obligatoria en Vercel
MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/?retryWrites=true&w=majority

# Opcionales (valores por defecto entre paréntesis)
# MONGODB_DATABASE_NAME=quickmarket
# RUNTIME_CONFIG_COLLECTION=runtime_config

# Si definís RESOLVER_SECRET, el GET exige ?token=... o Authorization: Bearer ...
# RESOLVER_SECRET=tu_secreto_de_pruebas

# Origen CORS; por defecto * (en producción podés restringir)
# CORS_ORIGIN=https://tu-app-web.com
