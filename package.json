{
  "name": "backend-base-url-resolver",
  "version": "1.0.0",
  "private": true,
  "description": "Vercel serverless API: resuelve la base URL del backend (ngrok) desde MongoDB Atlas",
  "scripts": {
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "mongodb": "^6.12.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "@vercel/node": "^3.2.29",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=18"
  }
}
